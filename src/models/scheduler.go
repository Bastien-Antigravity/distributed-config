package models

// Scheduler Capability
// -----------------------------------------------------------------------------

type SchedulerCapability struct {
	IP   string `yaml:"ip"`
	Port string `yaml:"port"`
}
