[package]
name = "distconf"
version = "1.9.6"
edition = "2021"
authors = ["Bastien-Antigravity"]
description = "Rust wrapper for the Distributed Config CGO bridge"

[dependencies]
libc = "0.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
libloading = "0.8"
